# Issue #24 生产配置边界专用环境运行手册

状态：**Ready to schedule / Pending dedicated-environment evidence**

本手册只负责把真实卷容量耗尽和只读卷验证约束为可恢复、可脱敏的专用环境会话。自动化 CI 与 `-ValidateOnly` 只能证明入口、依赖和安全合同存在，不能证明真实卷行为，更不能关闭 Issue #24。

## 1. 场景与边界

| ID | 场景 | 必须观察 | 当前状态 |
|---|---|---|---|
| I24-01 | 独立测试卷真实容量或配额耗尽 | 候选版本不发布；已提交主/备份保持可读取；恢复容量后可重试；没有残留未知写租约 | Pending |
| I24-02 | 独立测试卷被设置为只读 | 写入明确失败且不覆盖现场；读取最近有效版本或进入明确安全模式；恢复可写后可重试 | Pending |

本轮不验证物理断电、磁盘缓存丢失、OneDrive、网络盘、FAT/exFAT、完整产品生命周期或 `LongGrid.Infrastructure`。这些能力不得根据本轮结果自动升级为 Pass。

## 2. 专用环境要求

- 仅使用可还原快照的 VM 或可丢弃测试机，以及可卸载的独立虚拟磁盘；不得使用系统卷、工作区卷、用户资料卷或唯一工作设备；
- 在卷仍可写且为空时，由操作者在卷根目录创建 `.longgrid-issue24-dedicated-volume-v1`，内容必须精确为 `LONGGRID-ISSUE24-DEDICATED-VOLUME-V1`；
- 测试卷不得包含真实 Long方格配置、桌面文件、用户文档、账户数据或其他业务数据；
- 测试前记录匿名环境标签、Windows build、架构、文件系统类别和应用 commit，但不得记录盘符、卷 GUID、序列号、完整路径、用户名或机器名；
- 在改变配额、容量或只读状态前，必须准备恢复方式并确认 VM/磁盘快照可用。

## 3. 入口预检

先在最新 `main` 完成锁定恢复、Release 构建和测试，然后执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File ./eng/Start-Issue24PersistenceBoundarySession.ps1 `
  -Scenario I24-01 `
  -OperatorId O1 `
  -TargetRoot '<dedicated-volume-root>' `
  -AcknowledgeDedicatedEnvironment `
  -AcknowledgeRecoveryPlan
```

I24-02 只替换 `-Scenario`。入口必须拒绝 Windows 系统卷、工作区同卷、UNC/网络根、子目录、缺少标记、缺少匿名操作员或缺少两项确认的会话。输出不得包含目标路径或卷标。

预检成功不执行写入、不消耗容量、不改变卷状态、不运行配置探针，也不产生 Pass/Fail。操作者随后必须使用 VM、VHD 或测试存储设施完成所选场景。

## 4. 执行纪律

### I24-01：真实容量或配额耗尽

1. 从干净快照开始，确认测试卷没有业务数据，并保留恢复所需的安全余量；
2. 使用专用存储设施设置受控配额或容量边界，不得通过填满系统盘制造条件；
3. 在测试卷仍可写时建立至少一个可复读的已提交版本与备份；
4. 让后续候选写入在真实容量/配额边界失败，记录有限错误类别、主/备份哈希是否不变、暂存物状态与写租约是否释放；
5. 恢复容量后重试，确认可成功提交且旧有效版本仍可解释；
6. 还原快照或移除测试卷，确认宿主和工作区未改变。

### I24-02：只读独立卷

1. 从干净快照开始，在可写状态建立可复读的主版本与备份；
2. 使用测试存储设施把独立卷切换为只读；不得通过修改真实用户目录 ACL 冒充只读卷；
3. 确认保存失败不发布候选、不清空主/备份，也不把空配置作为成功返回；
4. 确认读取行为是最近有效版本或明确安全模式，错误输出不泄漏路径、内容或设备标识；
5. 恢复可写状态并重试，确认写租约释放且后续保存可完成；
6. 还原快照或移除测试卷，确认宿主和工作区未改变。

任何无法证明测试卷独立、恢复计划有效、旧版本未变或写租约已释放的情况都记为 `Inconclusive`；不得通过重复成功覆盖首次失败。

## 5. 匿名证据模板

```text
Issue / 场景：#24 / I24-01 | I24-02
提交：
匿名操作者：O1-O9
日期：
Windows build / 架构 / 文件系统类别：
专用环境与快照确认：Yes | No
前置主/备份哈希标签：
实际步骤：
有限错误类别：
失败后主/备份/暂存/租约状态：
恢复后重试：
结果：Pass | Fail | Inconclusive
脱敏证据引用：
缺陷：
恢复确认：
```

只允许提交匿名标签、有限错误类别、布尔状态、计数、耗时和脱敏哈希。不得提交盘符、卷 GUID、序列号、完整路径、配置内容、用户名、机器名或截图中的个人信息。

## 6. 完成判定

I24-01 与 I24-02 必须分别具备可复读的专用环境原始证据、恢复确认和缺陷分级，才能把真实卷前置从 Pending 改为已观察。即使两项通过，`LongGrid.Infrastructure`、应用关闭排空和完整单实例激活仍属于首个生产切片，Issue #24 不因会话入口或单次环境结果自动关闭。
