# Issue #23 五人可用性测试就绪审计

审计日期：2026-08-13

基线：`main@508528b`（PR #174 已合入；main CI 31707838547 通过）

结论：**Scope approved / Ready to schedule / Results Pending / 不得关闭 Issue #23**

## 0. 2026-08-13 执行就绪复审

本轮按 [Stage 125 收尾执行计划](125-phase0-internal-rc-closeout-plan.md)进入 C1，并重新核对当前 App、launcher、测试任务、通过门槛和 Issue #23：

- 当前 App 仍保留一键建议/空白起点、安全引用/真实移动阻断、匿名方格与三项加入、拖放语义、两步撤销和恢复三态练习；冻结任务没有因后续正式工作区开发而失效；
- `eng/Start-Issue23UsabilitySession.ps1` 仍只接受 P1–P5、有限输入方式和主题，并为每人启动全新进程；
- `-ValidateOnly` 在本地和 PR/main CI 均通过，输出继续保持 `ResultsPending`；
- 启动器不记录身份、不截屏、不枚举桌面、不写结果文件；计划和主持人手册禁止姓名、账户、路径、文件名、录音与录像；
- D23-01–D23-10 已批准，D23-11 许可证继续延期；C1 当前没有新增产品决策或工程功能缺口；
- 唯一未满足条件是 5 位未参与设计的真实参与者及其同一 commit 的独立会话结果。开发者、CI、UIA 或单人重复执行都不能替代该条件。

因此本次复审只确认 **工程就绪 Conditional Pass**，没有产生 P1–P5 结果，也没有改变 Issue #23 的 OPEN 状态。

## 1. 就绪范围

现有匿名 App 原型已经覆盖测试计划所需的核心心智模型：建议/空白起点、安全引用/移动阻断、创建方格、加入三个匿名引用、拖放动作语义、两步撤销，以及恢复差异三态、过期和取消。

本阶段没有继续增加演示功能，而是补齐执行入口：

- `eng/Start-Issue23UsabilitySession.ps1` 统一验证依赖并启动全新 Long方格进程；
- 主持人只能传入 P1–P5、输入方式和主题等有限匿名标签；
- 会话清单只输出 commit、Windows 版本、架构和测试参数；
- 启动器不写结果文件、不截屏、不枚举桌面、不收集个人数据；
- `-ValidateOnly` 固定输出 `ResultsPending`，只证明会话链可启动。

## 2. 隐私与证据合同

| 项目 | 合同 |
|---|---|
| 参与者 | 仅 `P1`–`P5`，仓库内不得存在身份映射 |
| 环境 | commit、Windows build、架构、输入方式、主题；缩放/分辨率由主持人手工匿名记录 |
| 禁止采集 | 姓名、账户、文件名、路径、截图、录屏、录音、设备硬件标识 |
| 结果写入 | 启动器不自动写文件；主持人按计划手工记录行为证据 |
| 会话隔离 | 每位参与者全新进程；关闭后内存状态自然清空 |
| 自动证据 | 只能证明启动链与隐私合同可用，不能证明任何用户任务通过 |

## 3. 自动化门禁

CI 新增 Issue #23 会话链 `-ValidateOnly`：

- 必须找到 App 启动器、测试计划和主持人手册；
- 隐私布尔值必须全部保持关闭；
- 结果状态必须保持 `ResultsPending`；
- 验证过程不启动窗口、不创建参与者结果或外部副作用。

## 4. 尚未完成

2026-08-03 首轮审计中记录的缩略图 worker 环境波动已由后续主线回归覆盖；当前 `main@508528b` 的完整 Windows CI 成功。这个自动化结论只关闭工程基线疑问，不提供任何可用性结果。

- 尚未招募或执行 P1–P5；
- 测试计划中的环境、时间、错误动作、提示次数和严重度仍为 Pending；
- 尚无五人汇总、中位时间、缺陷修复与重测结果；
- D23-01–D23-10 首发范围已由负责人批准，D23-11 许可证延期；本审计仍缺的是 P1–P5 真实结果，不得用范围批准替代可用性通过；
- Issue #19、#20 的真实输入、Narrator 和显示硬件矩阵不由本测试替代。

## 5. 下一动作

安排 5 位未参与设计的真实参与者，使用[主持人运行手册](usability/issue-23-facilitator-runbook.md)在同一审计 commit 上完成五个独立会话。会话前将该 commit 填入测试计划环境表；中途若因 Critical/High 合并修复，旧样本作废并从新 commit 重跑 P1–P5。只有匿名结果通过隐私复核、缺陷完成分级并写回 Issue #23 后，才能重新审计 Phase 0 产品/体验出口。
