# Issue #24 专用环境就绪审计

审计日期：2026-08-04

结论：**Session contract ready / Dedicated-volume results pending / 不得关闭 Issue #24**

## 1. 需求对齐

Issue #24 要求在隔离持久化探针和正式 v1 schema 之后补齐真实卷容量耗尽、只读行为与生产边界。本切片只解决“如何安全启动和记录专用环境会话”，不提前创建 `LongGrid.Infrastructure`，不把 P0-06 探针改名为产品模块，也不跨越 Issue #23、#19、#20 和 ADR-0001 的进入顺序。

## 2. 已建立的合同

- `eng/Start-Issue24PersistenceBoundarySession.ps1` 固定只接受 I24-01/I24-02 与匿名 O1-O9；
- 真实会话必须确认专用环境和恢复计划，并使用带固定标记的独立卷根目录；
- 启动器拒绝 Windows 系统卷、工作区同卷、UNC/网络根、子目录、缺少标记或缺少确认的会话；
- 输出固定为 `PendingDedicatedEnvironmentEvidence`，不包含目标标识；
- 启动器只读取标记，不写卷、不填盘、不改变卷状态/ACL、不运行探针、不截图、不写结果文件；
- CI 只执行 `-ValidateOnly`，证明依赖与安全合同未漂移，不产生真实卷证据。

## 3. 风险与停止规则

以下任一条件不满足时停止：

- 目标不是可丢弃的独立测试卷，或与工作区位于同一卷；
- 没有 VM/测试机快照和明确恢复计划；
- 目标包含用户、桌面、真实配置或业务数据；
- 需要管理员权限但无法解释精确操作范围；
- 输出或证据将泄漏盘符、卷 GUID、路径、序列号、用户名、机器名或配置内容；
- 无法证明失败后主/备份未被覆盖、写租约释放或环境已恢复。

## 4. 尚未完成

- I24-01 与 I24-02 尚未在专用 VM/测试卷执行；
- 物理断电、缓存丢失、非 NTFS、网络/云盘仍未批准进入本轮；
- 本报告创建时正式 `LongGrid.Infrastructure` 适配器、App 关闭排空和完整单实例激活尚未实现；后续切片现已完成这些能力，以及恢复状态 UI、显式恢复动作、当前 v1 本地 JSON 受限导入/导出、匿名证据清单和单项原始证据导出；正式迁移等待真实 v2 字段准入，证据保留/清理、真实产品状态入队与本报告的真实卷证据仍 Pending；
- 会话入口通过不等于真实卷通过，更不等于产品持久化完成。

## 5. 下一动作

按照[专用环境运行手册](manual-testing/issue-24-persistence-boundary-runbook.md)在同一 commit 上分别执行 I24-01 与 I24-02，向 Issue #24 回写匿名原始证据和恢复确认。真人可用性与人工/硬件矩阵完成并更新 ADR-0001 后，再在首个只读 MVP 垂直切片中建立正式 Infrastructure 适配器。
