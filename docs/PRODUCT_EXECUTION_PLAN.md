# Long方格产品重对齐与统一开发计划

版本：2.0

状态：Required / 当前唯一执行计划

更新日期：2026-08-26

代码审计基线：`origin/main@53c8b5e`；PF-007A2 实现分支 `codex/pf-007a2-drop-target`

界面参考基线：`Longyuyeee/long_Decompress@0362211af9f93e64149cf5574ad03cf3e4f7c2b6`

## 1. 本文的权威范围

本文解决 Long方格经历大量阶段开发后，状态、界面和下一步分散在多份文档中的问题。从本文生效起：

- [产品需求文档](02-product-requirements.md)决定“做什么、为什么做、MVP 不做什么”；
- 本文决定“现在做到哪里、下一步只做什么、以什么用户结果验收”；
- [交互设计规范](09-interaction-design-audit.md)决定页面、状态和操作反馈；
- [开发流程](10-development-workflow.md)决定实现、测试、审查和交付方法；
- [Stage 153 功能清单](153-product-feature-parity-development-plan.md)保留 PF 编号和竞品细项，降级为详细 backlog，不再承担当前状态与执行顺序；
- 其他编号 Stage 文档均为历史证据，不得用文档数量代表产品进度。

若状态冲突，以代码与测试事实为准，并在同一 PR 更新本文。任一时刻只能有一个“当前执行项”。

## 2. 统一产品定义

Long方格是 Windows 10/11 桌面盒子管理与任务栏美化软件。产品核心只有三根支柱，必须先把三者做完整，再扩展外围能力：

1. **桌面盒子**：用户在 Windows 桌面空白处右键创建盒子，并直接在桌面移动、缩放、折叠、锁定、重命名、删除和调整外观；
2. **文件夹绑定**：盒子可以绑定一个真实文件夹，在桌面显示和管理该文件夹内容，并清楚处理刷新、离线、权限和失效路径；
3. **任务栏美化**：用户通过可视化预设改变受支持的系统任务栏透明度、颜色和材质，并能可靠恢复系统默认。

Explorer 拖放、盒子间改归属和盒子属性属于上述核心的必要交互。自动整理、Tab、Peek、工作空间、小组件和 Long助手插件均为建立在三根支柱之上的扩展功能，不得再次抢占核心闭环。

产品承诺保持不变：本地优先、轻量常驻、零惊吓、不注入 Explorer、不擅自移动或删除用户文件。首版默认使用安全引用模式。

## 3. 2026-08-25 统一审计结论

### 3.1 已有工程资产

- WinUI 3 应用、单实例、配置存储、恢复、撤销、导入导出和一键启动/打包链已经存在；
- DesktopHost 已具备方格创建、布局、标题操作、图标/缩略图、选择、键盘导航、安全打开和框选等正式工程路径；
- PF-001～PF-006 已达到 `EngineeringComplete / ProductEvidencePending`；
- PF-007A1 已完成 `CF_HDROP` 有界解析、安全准入和一次原子配置请求；
- 文件安全、失败关闭、CAS 持久化、恢复和自动化门禁是当前最可靠的资产，必须保留。

### 3.2 UI-R1A 前不是合格软件体验的原因

UI-R1A 开始前的代码审计显示，正式控制中心仍沿用早期“开发期只读 UI Shell”的信息架构：

- 一级导航为“概览、首次整理、外观与 Token、安全边界、恢复预览”；
- 首屏反复展示“只读、尚未连接、不会修改、等待会话、证据清单、仍需关闭的门槛”；
- 首次整理仍包含匿名内存练习和拖放语义教学；
- Design Token、开发状态和安全审计被直接当成面向用户的产品模块；
- 真实方格管理能力被大量说明文字、状态卡和原型入口包围；
- `MainWindow.xaml` 约 141 KB，`MainWindow.xaml.cs` 约 231 KB，页面、原型、诊断和业务协调过度集中。

因此用户看到的是“开发教程和验证控制台”，不是桌面管理软件。安全边界本身没有错，错误在于把内部工程语言长期放在产品主路径中。

### 3.3 进展缓慢的根因

1. 以工程切片和 Stage 文档计数，而不是以完整用户旅程计数；
2. 自动化和安全底座持续扩展，但物理可见产品闭环长期为 Pending；
3. 原型页面没有在能力成熟后退出正式导航；
4. `MainWindow`、`App`、DesktopHost Surface 和 Lifecycle 类持续膨胀，修改成本逐步增加；
5. 当前状态被 README、Stage 153、各最新审计和聊天重复维护，出现互相矛盾；
6. 每项功能继续细分 A/B/C 阶段，却没有设置“用户可以完成整个任务”的强制出口。

这属于执行偏移，不是产品愿景偏移。后续应保留安全工程成果，但立即重建产品壳层和交付节奏。

### 3.4 2026-08-25 审计与 2026-08-26 GitHub 集成收口

首次审计以当时的 `origin/main@978569d` 和 UI-R1E 分支分别判断；2026-08-26 已完成集成收口。下表保留能力事实并更新主分支结果，继续禁止把工程底座写成用户旅程已完成：

| 审计面 | 主分支事实 | 最新开发分支事实 | 结论 |
|---|---|---|---|
| 产品 UI | `origin/main@288838c` 已包含 UI-R1A～UI-R1E-B 工程范围 | 产品导航、真实概览、盒子管理、设置分层、显式启动入口和真实 XAML 渲染证据均已进入主线 | 工程范围完成；高对比、减少动画、Narrator 和物理键盘仍为 `ManualEvidencePending`，不能把 UI-R1 整体标为 Complete |
| 桌面右键创建 | DesktopHost 已具备创建事务、预览、快捷键、拖画入口和产品内弹出菜单 | 右键“新建方格”当前仅在宿主绘制的“新建”按钮区域触发，不是 Explorer 桌面任意空白区域 | BOX-R1 仍未达到原始核心旅程，必须通过受支持的 Explorer 背景命令补齐入口、冲突处理和物理证据 |
| 文件夹绑定 | 有安全引用、目录枚举、打开与 Portal/Tab 设计底座 | FOLDER-R1-A/B 已建立 schema v3、稳定身份和绑定事务；C 已将最多 256 个直属项目投影到盒子管理页和 DesktopHost；D 已让运行时快照发布权威有限绑定状态，并以根目录/父目录事件与低频健康探测处理权限拒绝、离线、归来和同路径替换 | A～D 工程链已通过真实文件系统；物理 Picker、可见刷新/打开、键盘和 Narrator 仍 `ProductEvidencePending`，不得写成完整用户旅程已完成 |
| 任务栏美化 | 有技术审计和产品边界文档 | `DwmSetWindowAttribute` 只修饰 Long方格自己的宿主窗口；个性化页明确仍为适配器占位文案 | TASKBAR-R1～R4 尚无产品运行时，M2 未开始 |
| Explorer 拖放 | PF-007A1 已完成 `CF_HDROP` 有界解析、安全准入和原子配置请求 | PF-007A2 已在正式 DesktopHost HWND 注册/撤销 OLE `IDropTarget`，只对未锁定盒子发布 Link，并接入 App 权威 Catalog、原子保存、补偿和一次撤销链；真实 STA/HWND/OLE 注册与文件安全通过 | A2 `EngineeringComplete / RealOleRegistrationPass / ProductEvidencePending`；盒子间改归属仍待 B，物理 Explorer 指针拖入留在 M1 集中证据 |
| 小组件/Long助手插件 | LPWP 1.0 等协议文档存在 | 只有 Windows App SDK Widgets 的传递依赖，没有 Widget Host 或插件小组件运行时 | 方向对齐但仍是 M4；不得抢占三根核心支柱 |
| 启动与打包 | 根目录 `启动Long方格.cmd` 和 `eng` 启动、打包、MSIX、RC/SBOM 脚本存在 | 内部未签名链可用，正式签名和发布矩阵仍待 M5 | 工程入口基本对齐，发布能力未闭环 |

GitHub 集成结果：#238 已注明被 #239 覆盖并关闭；#239、#240、#241、#242 均在 Clean、CI `build-test` 成功的前提下依次合并。#240～#242 每次先改为最新 `main` 基线并复核只包含本阶段差异，最终 `main=dc06857`。开放 UI PR 栈已清零，后续不得再用长期堆叠分支代替主线集成。

代码结构风险仍然偏高：`MainWindow.xaml.cs` 5,289 行、`App.xaml.cs` 4,053 行、`WindowsProductDesktopHostReadOnlySurface.cs` 3,391 行。后续在 BOX-R1/FOLDER-R1 的用户旅程内就近提取页面协调器、创建/绑定服务与原生输入适配器；不得另开长期“纯重构阶段”，也不得继续把新业务集中进上述三个文件。

本次没有发现产品目标再次偏移；“开放分支被当成已交付”的集成偏移已经纠正，“底座成熟度被当成核心旅程完成度”的口径偏移继续由本文约束。后续严格保持 BOX-R1 → FOLDER-R1 → PF-007A2/B → M1 集中证据（含 UI-R1E 人工矩阵）→ TASKBAR-R1～R4，不插入自动整理、小组件或新协议功能。

## 4. Long_Decompress 界面参考审计

参考仓库：[Longyuyeee/long_Decompress](https://github.com/Longyuyeee/long_Decompress)。本次实际检查了其 `MainLayout.vue`、路由、Design Token、动效样式和仓库截图。

值得统一到 Long 产品家族的语言：

- 左侧固定品牌区，品牌图标、产品名和一句产品定位清晰可见；
- 一级导航数量有限、名称均为用户任务，不出现技术术语；
- 当前模块使用强调色、图标和左侧指示条，不只依赖颜色；
- 页面顶部使用“大标题 + 一句话说明 + 右侧唯一主操作”；
- 主内容区直接呈现真实任务、文件、进度或设置，不先展示教程；
- 浅色/深色、卡片、边框、圆角、阴影和强调色由统一 Token 控制；
- 180～300 ms 的可中断页面和控件动效，减少动画时可关闭；
- 窄窗口将侧栏压缩成图标栏，而不是产生横向滚动；
- 状态、进度和隐私承诺放在稳定的次级位置，不遮挡主任务。

不应照搬的部分：

- 不复制解压业务的信息密度、任务队列结构或暖黄色配色；
- 不把每个未来功能预先放入导航；未实现模块应隐藏，不显示大量禁用占位页；
- 不照搬 Web/Tauri 技术实现，Long方格继续使用 WinUI 3 和 Windows 原生无障碍能力；
- 不使用持续模糊、强发光或大面积动画增加空闲资源占用。

统一设计方向定义为：**Long 产品家族化、现代扁平、精致华丽、直接可用、低噪声**。Long方格使用更偏冷静的靛蓝/蓝紫强调色和桌面空间感，保持与 Long解压同族但能被清楚区分。

## 5. 产品表面与职责

### 5.1 桌面表面：第一产品主角

桌面方格承担 80% 的高频行为：

- 桌面空白处右键创建盒子，并支持拖动、缩放、折叠、锁定、重命名和删除；
- 将盒子绑定/解绑到一个真实文件夹，显示绑定位置和当前健康状态；
- Explorer 拖入、方格间拖放、选择、框选、排序和打开；
- 当前动作反馈、失败原因和一次撤销；
- 快速隐藏、Peek、搜索和 Portal 等后续高频能力。

用户完成普通整理时不应被迫打开控制中心。

### 5.2 控制中心：管理与配置

控制中心采用稳定侧栏，MVP 导航固定为：

1. **桌面概览**：盒子数量、绑定文件夹、当前状态、最近动作和“新建盒子”；
2. **盒子管理**：真实盒子列表、文件夹绑定、搜索、排序、属性和批量管理；
3. **个性化**：盒子主题、标题、透明度、动效，以及核心任务栏美化预设与恢复；
4. **布局与恢复**：显示器布局、快照、差异预览和恢复；
5. **自动整理**：扩展规则、建议预览、冲突和撤销；未交付前不进入发布构建导航；
6. **设置**：启动、快捷键、行为、隐私、插件、诊断和关于。

小组件在运行时具备真实管理能力后增加独立模块。单文件夹绑定是核心盒子类型；多目录标签、复杂 Portal 导航和高级文件操作属于后续增强，不为单一类型新增顶级导航。

### 5.3 退出主导航的现有内容

| 现有内容 | 新归属 |
|---|---|
| 首次整理匿名练习 | 仅首次启动的 2～3 步引导；完成或跳过后不常驻导航 |
| 外观与 Token | 改为“个性化”，只展示用户能立即应用的真实设置 |
| 安全边界 | 设置 → 隐私与安全；危险动作在发生位置显示上下文确认 |
| 恢复预览匿名场景 | 布局与恢复中的真实快照/差异状态 |
| 证据清单、门槛、开发期状态 | 设置 → 高级诊断；默认折叠，发布版不展示工程术语 |
| 开发期只读 UI Shell 标记 | Debug 构建标题/关于页；不得占用正式标题栏品牌区 |
| Design System 样例卡 | 移出产品 UI，保留到开发预览或测试页面 |

## 6. 页面和视觉基线

### 6.1 桌面概览首屏

打开应用后必须在 3 秒内回答三个问题：我有多少方格、当前是否正常、下一步最常用动作是什么。

推荐结构：

```text
┌ 品牌侧栏 ─────────┬ 桌面概览                         [＋ 新建方格] ┐
│ Long方格          │ 管理桌面上的方格、项目与布局                 │
│ 桌面概览          ├─────────────────────────────────────────────┤
│ 方格管理          │ [6 个方格] [128 个项目] [自动整理：已暂停]  │
│ 自动整理          │                                             │
│ 布局与恢复        │ 我的方格                                    │
│ 个性化            │ [项目 A] [待处理] [常用工具] ...            │
│ 设置              │                                             │
│                   │ 最近动作：3 个项目已加入“项目 A” [撤销]    │
└───────────────────┴─────────────────────────────────────────────┘
```

首屏禁止出现长段开发说明。安全引用只在首次引导、拖放反馈和帮助中用一句人话说明，例如“只加入引用，不会移动原文件”。

### 6.2 设计令牌

- 品牌强调色：以现有 `#5B5FF5` 靛蓝为基准，补齐 hover、pressed、selected 和高对比映射；
- 背景：系统 Mica/纯色基底；卡片使用轻量 Surface，不叠加多层 Acrylic；
- 文字：页面标题 28～32、模块标题 18～20、正文 14、辅助 12；正文不使用全大写工程标签；
- 间距：4/8/12/16/24/32 的 8pt 主网格；
- 圆角：控件 8、卡片 12、主要容器 16；
- 阴影：只表达浮层、拖拽和选中提升，不让每张卡片长期悬浮；
- 图标：统一 Fluent Symbol 或 Long 自有几何图标，不混用 Emoji；
- 动效：hover 120～160 ms，页面/面板 180～240 ms，布局恢复不超过 300 ms；必须可中断并服从减少动画；
- 响应式：宽度小于约 840 effective px 时侧栏压缩为图标模式，内容保持无横向滚动；
- 主题：浅色、深色、跟随系统、高对比均为必测状态。

### 6.3 状态语言

每个主页面只实现与真实数据绑定的：Loading、Empty、Ready、Partial、Error、Recovery。错误说明“发生了什么、是否改变文件、现在能做什么”。开发名词如 revision、generation、CAS、UIA、HDROP 不进入普通用户文案。

## 7. 需求对齐总账

| 原始需求 | 当前事实 | 目标里程碑 |
|---|---|---|
| 桌面右键创建盒子与基本管理 | PF-001～PF-006 有大量工程链；右键菜单目前只覆盖宿主“新建”按钮区域，桌面任意空白处创建与完整物理旅程待补 | M1 |
| 盒子绑定文件夹 | 只有引用、目录枚举和 Portal 设计底座，真实绑定盒子未闭环 | M1 |
| Explorer 拖入和方格间整理 | PF-007A1 基础完成，真实 DropTarget/方格间拖放未完成 | M1 |
| 正常、现代的软件 UI | UI-R1A～UI-R1D 已完成产品结构；UI-R1E-A 已取得真实浅/深色、宽/紧凑渲染并修正标题重叠，高对比/减少动画仍待补 | M1，持续纠偏 |
| 任务栏美化 | 竞品/技术边界文档存在，运行时和设置 UI 未实现 | M2，核心支柱 |
| 自动分类整理 | 未形成真实规则用户闭环 | M3 扩展 |
| 快速隐藏、托盘、快捷键 | 未完成 | M3 扩展 |
| 布局快照与多显示器恢复 | 底座较强，完整产品页和用户证据未闭环 | M3 扩展 |
| 多目录 Portal、Tab、工作空间 | 未实现 | M3/M4 扩展 |
| 桌面小组件 | 协议设计存在，Widget Host 未实现 | M4 |
| Long助手插件转小组件 | LPWP 1.0 文档存在，双仓互操作未实现 | M4 |
| L + 方格品牌图标 | RC1 资产存在 | M1 视觉验收 |
| 一键启动和打包 | 工程脚本与根目录启动入口存在，正式签名缺失 | M1/M5 |
| 容量、速度、稳定性 | 自动化底座较强，真实长期用户证据不足 | 全程门禁 |

## 8. 里程碑与严格出口

### M1：桌面盒子与文件夹绑定核心

目标：用户启动后看到正常控制中心，并能在真实桌面完成“右键创建盒子 → 绑定文件夹 → 调整盒子设置 → 浏览/打开内容 → 拖入项目 → 撤销”。

必须交付：

- UI-R1 产品壳层与信息架构重构；
- UI-R2 桌面概览和方格管理绑定真实工作区；
- BOX-R1 桌面右键菜单创建盒子并进入统一创建事务；
- FOLDER-R1 单文件夹绑定、解绑、刷新、打开和健康状态；
- PF-007A2 DesktopHost 真实 `IDropTarget`；
- PF-007B 方格间拖放与一次撤销；
- PF-001～PF-007 关键物理鼠标、键盘、Narrator、高对比和截图证据；
- 根目录一键启动、Debug/Release 构建继续通过。

出口：一名未读过开发文档的测试者可在 2 分钟内完成上述旅程；主界面不出现开发教程或虚假能力；绑定文件夹不会被静默搬移；所有操作前后真实文件哈希和位置符合预期。

### M2：任务栏美化核心

目标：像 iTop 一样用简单预设完成任务栏美化，同时把 Windows 版本风险、冲突和恢复做得更可靠。

必须交付：TASKBAR-R1 兼容性探测和独立故障域；TASKBAR-R2 系统默认/清透/深色半透明/浅色半透明/Acrylic（仅支持时）预设；TASKBAR-R3 15 秒确认、自动回退、Explorer 重启恢复、冲突检测和“一键恢复系统默认”；TASKBAR-R4 Windows build、多显示器、全屏、高对比和卸载恢复真实矩阵。

出口：用户不理解内部 API 也能选择、预览、保留或撤销预设；不支持的 Windows build 明确拒绝；崩溃、Explorer 重启、禁用和卸载均能恢复系统默认；不注入或 Hook Explorer。

M1 和 M2 同时完成，才可以称为“Long方格核心功能完成”。

### M3：桌面管理增强

目标：在三根核心支柱稳定后增加自动整理和日常效率能力。

必须交付：PF-008～PF-011、PF-020～PF-022、PF-030～PF-033、PF-041～PF-042 高级 Portal、PF-050～PF-052，以及完整搜索、排序、视图和布局档案。

出口：自动建议可解释并可撤销；Portal 权限/离线/重命名/拖放状态可恢复；多显示器真实矩阵通过。

### M4：工作空间与生态

目标：在核心稳定后吸纳 iTop 的产品宽度，但保持隔离和可恢复。

必须交付：PF-060 应用工作空间、PF-070 视觉精修、Widget Host、LPWP 1.x Long助手互操作、插件权限、崩溃隔离、兼容矩阵和一键禁用。

出口：关闭实验后 Windows 外观完全恢复；Widget/插件崩溃不影响 DesktopHost；Long助手与 Long方格运行相同 Golden Fixtures；未实现能力不在正式导航中占位。

### M5：可分发产品

目标：完成 PF-071、PF-090、签名安装、升级/卸载、隐私与支持闭环。

出口：受保护签名、安装/升级/卸载、SBOM、哈希、崩溃恢复、性能和辅助功能门禁全部通过，才允许 Beta/Stable。

## 9. 当前唯一执行队列

当前执行项：**PF-007B 盒子间改归属**。PF-007A2 已完成正式 DesktopHost `IDropTarget` 的注册、Link-only 悬停/投递、App 权威准入和现有原子保存/补偿/撤销接线，为 `EngineeringComplete / RealOleRegistrationPass / ProductEvidencePending`。物理 Explorer 指针拖入，连同 BOX-R1-C、FOLDER-R1 和 UI-R1E 人工门禁，统一留在 M1 产品证据冲刺。

严格按下列顺序交付，不再插入相邻探针或新宽度功能：

1. **UI-R1A 壳层拆分（EngineeringComplete / ProductEvidencePending）**：已提取品牌头与 PageHeader 组件，建立“桌面概览 / 盒子管理 / 个性化 / 设置”导航，移除开发期徽标，并将匿名示例与 Design Token 样例移出产品表面。为避免再次以架构重构替代用户结果，本轮没有先行抽空整个 AppShell；`MainWindow` 继续拆分归入各后续页面交付。
2. **UI-R1B 桌面概览（EngineeringComplete / ProductEvidencePending）**：已使用真实 workspace presentation 提供盒子数、项目数、健康状态、最多四个盒子摘要、空状态和最近撤销；旧 Core/文件策略/宿主/代次工程卡退出首屏。真实空工作区已核对，非空盒子与撤销可见证据随 UI-R1C 创建旅程补齐。
3. **UI-R1C 盒子管理（EngineeringComplete / ProductEvidencePending）**：已将页面重排为盒子列表与就近设置两栏，窄窗口改为上下排列；基本设置默认展开，外观与布局、删除与恢复、盒子内容按需展开；用户文案统一为盒子/项目，并删除匿名工作区样例。隔离真实存储旅程已通过，可见键鼠证据待 UI-R1E 补齐。
4. **UI-R1D 设置迁移（EngineeringComplete / ProductEvidencePending）**：配置导入导出归入“常规”，隐私承诺使用用户语言，一次性匿名证据归入默认折叠的“高级诊断”；开发门槛和 Design Token 样例已从发布表面删除。真实磁盘往返与损坏输入保护通过，可见交互证据随 UI-R1E 完成。
5. **UI-R1E 视觉与响应式验收（EngineeringComplete / ManualEvidencePending）**：UI-R1E-A 已完成浅/深色、1120/720 effective px、200% 缩放、焦点和真实 XAML PNG；UI-R1E-B 已确保根目录/显式启动直接显示唯一控制中心，同时保留 `--background` 桌面优先模式。I19/BSA 自动预检均通过且明确不修改系统设置；高对比、关闭系统动画、Narrator、物理键盘及其他 DPI 继续作为 M1 出口人工门禁。
6. **BOX-R1（B 已完成，C Pending）**：A 有限激活合同（EngineeringComplete / RealProcessPass）→ B 原生 `IExplorerCommand` 与真实 unsigned MSIX（EngineeringComplete / NativeDllPass / RealUnsignedPackagePass）→ C 可丢弃账户真实安装/菜单/卸载证据（PendingApproval）→ D Explorer 重启、显示器、DPI 与失败恢复；全部复用统一创建预览和事务。
7. **FOLDER-R1（A～D EngineeringComplete / RealFilesystemPass / ProductEvidencePending）**：A 持久化/身份/健康合同 → B 显式选择、取消零写入、绑定/解绑/重连原子提交 → C 目录内容呈现、显式刷新、变更通知和安全打开 → D 权限、离线、同路径替换、失效恢复和有限状态；物理 Picker、可见刷新/打开、键盘和 Narrator 进入 M1 集中证据。
8. **PF-007A2（EngineeringComplete / RealOleRegistrationPass / ProductEvidencePending）→ PF-007B（当前执行）**：A2 已完成正式 HWND OLE DropTarget 和安全 Link 引用提交；B 完成盒子间改归属、无效目标、失败补偿和一次撤销。
9. **M1 产品证据冲刺**：完整两分钟旅程，而不是分散控件证据。
10. **TASKBAR-R1～R4**：完成核心任务栏预设、应用、回退和真实兼容矩阵，关闭 M2。

UI-R1 允许重构产品壳层，但不得重写已经验证的 Core、配置事务和 DesktopHost 安全边界。

### 9.1 UI-R1A 真实验收记录

测试环境：Windows `10.0.26200`、240 DPI / 250% 缩放、浅色主题、Release x64；正式 `LongGrid.App.exe`，真实单实例重定向启动。

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 产品导航 | 一级入口只表达用户任务 | 实际显示“桌面概览 / 盒子管理 / 个性化 / 设置”，开发期徽标、首次整理、安全边界、恢复预览均退出一级导航 | 对齐；161-ID 静态 UI 合同通过 |
| 页面头 | 品牌清晰，页面只有一个主要动作 | 实际渲染 Long方格品牌、桌面概览标题与“新建盒子”主动作；正常启动不再弹出只读开发说明 | 对齐；新建动作进入真实盒子工作区并聚焦名称编辑器 |
| 原型降噪 | 匿名示例与 Token 展示不出现在正式产品表面 | UI-R1C 已删除匿名工作区样例；Design Token 样例继续折叠，等待 UI-R1D 移出设置表面 | 对齐当前阶段 |
| 正文产品化 | 首屏显示真实盒子数量、摘要与最近动作 | 正式窗口仍显示“Core 只读已接线、文件操作安全策略”等工程状态卡 | 未对齐；禁止把 UI-R1A 误报成完整 UI，UI-R1B 唯一目标即替换这些卡片 |
| 真实稳定性 | Release 控制中心可见且持续响应 | 标题“Long方格”的进程连续响应超过 166 秒，正常关闭；0 个桌面文件操作 | 对齐本轮启动范围 |
| 可见/无障碍证据 | 自动读取窗口并完成键盘/屏幕阅读器核对 | 无 UIA 的真实窗口捕获成功；跨进程 UIA 触发 `Microsoft.UI.Xaml.dll` `0xc000027b` / WER `8001010e`，与 Stage 169/181 已知上游签名一致 | 未伪造通过；UI-R1E 在安全运行时补测，不通过绕过安全预检解决 |
| Release 编译 | 全解决方案可生成 | 首次错误地传入不存在的 `Release|x64` 解决方案配置并被拒绝；改用仓库真实 `Release|Any CPU` 后 0 warning / 0 error | 测试命令已修正，不修改解决方案配置 |
| 核心测试 | 既有行为无回归 | 首轮 1232/1233；真实缩略图 worker 为 1533 ms，超过 1500 ms 返回 `TimedOut`；同项隔离复测 875 ms 通过，全量顺序复测 1233/1233 | 记录为环境时延抖动；未放宽超时或伪造结果，后续性能门禁继续观察 |
| 格式与合同 | 代码格式、导航和隐藏规则稳定 | `dotnet format --verify-no-changes` 通过；UI contract-only 通过 | 对齐 |

文件安全：本轮仅启动/关闭正式应用并读取窗口像素；未执行盒子提交、文件夹绑定、拖放、移动、重命名、删除或桌面配置写入。可见截图只保存在本机临时目录，不提交包含本机内容的图像。

### 9.2 UI-R1B 真实验收记录

测试环境：Windows `10.0.26200`、240 DPI / 250% 缩放、浅色主题、Release x64；正式 `LongGrid.App.exe`，本机真实空工作区。

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 空工作区统计 | 没有保存盒子时显示 0 个盒子、0 个项目，不显示假数据 | 正式窗口实际显示“桌面盒子 0”“盒子项目 0” | 对齐；数据来自 `ProductWorkspaceReadPresentation`，没有匿名示例 |
| 首屏内容 | 工程状态卡退出，显示用户可理解的统计、盒子摘要和最近操作 | 实际首屏未出现“Core 只读已接线 / 文件操作安全策略 / Desktop Catalog 代次” | 对齐；六张旧工程卡由 UI 合同强制保持折叠 |
| 盒子摘要 | 有盒子时最多显示四个真实盒子，更多内容进入盒子管理 | 本机没有已保存盒子；空状态路径已真实渲染，非空映射通过生产编译和合同 | 部分证据；UI-R1C 创建真实盒子后补充非空可见核对，不把静态绑定冒充真实结果 |
| 最近操作 | 有一次可撤销配置编辑时显示对应撤销动作 | 当前真实会话没有可撤销编辑，实际显示无操作状态；各种撤销 token 已接入同一 presentation | 部分证据；UI-R1C 真实创建/重命名后核对按钮、结果和保存状态 |
| 导航 | “管理全部”和空态“创建盒子”进入真实盒子管理/创建入口 | 两个入口均复用正式导航与名称编辑器处理器；177-ID 合同通过 | 跨进程 UIA 点击仍受已知上游崩溃阻断，真实键盘/鼠标补证归入 UI-R1C/UI-R1E |
| 真实稳定性 | 新概览在高 DPI 下持续响应并可正常关闭 | 进程标题“Long方格”，连续响应 146 秒并正常关闭 | 对齐 |
| 自动化回归 | 全解决方案、核心行为、格式和 UI 合同通过 | Release 0 warning / 0 error；核心测试 1233/1233；UI contract-only 177 IDs Pass | 对齐 |

文件安全：本轮实际读取本机空工作区并捕获产品窗口；没有创建盒子、修改配置、移动、重命名、删除或读取桌面文件内容。截图仅保存在本机临时目录。

### 9.3 UI-R1C 真实验收记录

测试环境：Windows `10.0.26200`、Release x64；真实 `ProductConfigurationStore` 临时隔离目录、真实磁盘配置文件和独立桌面保护文件；正式窗口自动化使用 Computer Use 安全链。

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 管理页结构 | 左侧选择/搜索盒子，右侧就近修改；次级功能不形成超长表单 | 生产 XAML 已形成两栏 Grid；基本设置默认展开，外观与布局、删除与恢复、盒子内容默认折叠；窄窗口进入第 7 行上下排列 | 工程对齐；182-ID UI 合同和 Release 编译通过，可见截图受下述上游问题阻断 |
| 创建后概览 | 从空状态创建 `Inbox` 后，真实读模型应出现 1 个盒子 | 实际配置事务接受创建；`ProductWorkspaceReadModel` 返回 1 个盒子，名称为 `Inbox` | 对齐，不使用 Mock 或匿名 UI 样例 |
| 重命名与撤销 | `Inbox` 重命名为 `Projects`，撤销后恢复 `Inbox` | 实际重命名事务接受并生成 undo token；撤销接受；从真实配置文件重载后的 fingerprint 与预期完全一致 | 对齐 |
| 文件安全 | 配置编辑不得改变桌面文件 | 在独立临时 DesktopGuard 写入真实文件；旅程前后 SHA-256 与文本内容完全一致 | 对齐；未访问用户真实桌面内容 |
| 真实旅程耗时 | 隔离创建/重命名/撤销/重载稳定完成 | 单项测试 378 ms 通过；全量核心测试 1234/1234 | 对齐 |
| 正式窗口可见核对 | 唯一 Long方格窗口应可读取并导航到盒子管理 | Computer Use 返回唯一“Long方格”窗口；读取窗口状态时应用退出，Application Error `0xc000027b`、WER `8001010e` | 与 Stage 169/181 已知 `RPC_E_WRONG_THREAD` 一致；立即停止输入，不把 XAML/编译误报成可见通过 |
| 构建与格式 | Release、格式与 UI 合同无回归 | 全解决方案 0 warning / 0 error；`dotnet format --verify-no-changes` 与 182-ID contract-only 通过 | 对齐 |

文件安全：隔离旅程只写入系统临时目录中的配置与保护文件，测试结束后由测试清理其精确沙箱；没有移动、重命名、删除或读取用户桌面文件。Windows UI 自动化在状态捕获失败后执行了 0 次点击、0 次键盘输入、0 次配置提交。

### 9.4 UI-R1D 真实验收记录

测试环境：Windows `10.0.26200`、Release；真实 `ProductConfigurationStore` 源/目标临时目录、真实导出 JSON、损坏 JSON 内存输入，以及独立桌面保护文件。

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 设置分层 | 普通用户先看到能立即操作的设置，工程诊断不占据主路径 | 设置页实际分为“常规 / 隐私与安全 / 高级诊断”；配置备份在常规，一次性匿名证据位于默认折叠的高级诊断 | 对齐；184-ID UI 合同强制分区、默认状态与原处理器不变 |
| 发布文案 | 不显示 Issue、ADR、证据门槛或设计系统样例 | “仍需关闭的产品门槛”和 `LegacyDesignTokenPreviewCard` 已从 XAML 删除；隐私区改为文件控制与主动操作说明 | 对齐；未删除底层安全限制，只调整用户表达和入口层级 |
| 真实配置往返 | 当前配置导出为非空文件，导入另一个真实存储后内容完全一致 | 实际生成独立 JSON 文件；目标存储加载成功；预期 fingerprint=`Compute(源文档)`，实际 fingerprint=`Compute(导入文档)`，两个 64 位 SHA-256 值严格相等 | 对齐；测试不使用 Mock、匿名内存仓库或伪造结果 |
| 损坏输入恢复 | 损坏 JSON 必须被拒绝，已导入配置保持不变 | 实际返回 `InvalidConfiguration`；重试前后目标主配置逐字节相同 | 对齐；没有产生部分写入或覆盖 |
| 文件安全 | 配置往返不得改变桌面文件 | 独立 DesktopGuard 文件在旅程前后逐字节相同，导出文件长度大于 0 | 对齐；仅写入并清理精确的系统临时测试沙箱 |
| 自动化回归 | 新结构不破坏既有能力 | 新真实旅程 1/1、完整核心测试 1235/1235；Release 0 warning / 0 error；184-ID contract、批量无障碍链、干净会话链和格式检查全部通过 | 对齐 |
| 可见视觉证据 | 浅/深/高对比、DPI 和窄窗口可实际观察 | 本阶段没有再次把静态 XAML 误报为实机视觉结果；已知跨进程 UIA 崩溃仍未关闭 | 未完成；UI-R1E 专门执行截图、键盘和响应式差异修正 |

### 9.5 UI-R1E-A 真实验收记录

测试环境：Windows `10.0.26200`、Release x64、`RasterizationScale=2.0`（200%）；正式 `LongGrid.App.exe`、唯一 GUID AppInstance 和隔离配置目录；DesktopHost 强制关闭。正式窗口标题和 HWND 实际存在，但 Computer Use 两次均未枚举到该 WinUI 窗口，因此停止外部输入，改由正式 App 进程内 `RenderTargetBitmap` 捕获真实 XAML。

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 浅色宽窗口 | 1120 effective px 使用宽布局，设置分区清晰 | 实际宽度 1107 DIP，`LayoutStatus=wide`、`PaneDisplayMode=LeftCompact`；常规/隐私卡宽 994 DIP，真实 PNG 可读 | 对齐 |
| 浅色紧凑窗口 | 720 effective px 使用紧凑布局，标题与汉堡按钮不重叠 | 首轮实际 707 DIP 且正确进入 `compact/LeftMinimal`，但 PNG 显示汉堡按钮侵入页面标题区域 | 已修正：断点由 760 对齐为 840 DIP，紧凑内容顶部安全区由 20 增至 56 DIP；第二轮 PNG 不再重叠 |
| 深色宽窗口 | 深色主题真实生效，结构与浅色一致 | `RequestedTheme=Dark`、`ActualTheme=Dark`，实际宽度 1107 DIP、布局仍为 wide；真实 PNG 显示文字、卡片、焦点和状态条 | 结构对齐；完整颜色对比率随 UI-R1E-B 高对比矩阵复核 |
| 默认诊断状态 | 高级诊断默认折叠 | 三个实际渲染状态均为 `AdvancedDiagnosticsExpanded=false` | 对齐 |
| 键盘焦点 | 设置页首个配置动作可取得可见键盘焦点 | 三个实际渲染状态均为 `FocusedElement=ImportConfigurationButton`，PNG 显示焦点边框 | 进程内真实焦点对齐；物理 Tab 顺序和 Narrator 留给 UI-R1E-B |
| 系统环境 | 记录而非伪造高对比和减少动画状态 | 实际 `HighContrast=false`、`AnimationsEnabled=true` | 高对比与关闭动画没有在本机发生，明确保持 Pending，不通过程序伪造系统设置 |
| 外部自动化 | Computer Use 应唯一选中正式窗口并读取状态 | 隔离 Release 进程实际存在标题 `Long方格` 和 HWND，但 `list_apps/list_windows` 连续两次返回 0 个候选 | 按技能要求停止输入；没有猜测句柄或坐标，也没有终止 8 月 21 日遗留的外来进程 |
| 证据链架构 | UI 窗口只负责渲染，文件写入留在证据边界 | 首次收紧边界时改用进程内 PNG 流，实际触发 `0xc000027b / 8001010e` 且 45 秒内无结果 | 未放宽或忽略失败；改为窗口返回真实 BGRA 像素、证据会话负责稳定文件流编码，重跑三图与 JSON 全部通过，UI 代码继续保持零文件 I/O |
| 文件安全 | 证据不得读取/修改桌面内容或正常配置 | 证据只渲染隔离空配置的产品 XAML；桌面目录元数据 fingerprint 和 `%LOCALAPPDATA%\LongGrid` 元数据 fingerprint 前后相同 | 对齐；PNG/JSON 只写 GUID 临时目录并支持精确清理 |
| 回归 | 实机修正不得破坏既有功能 | 真实渲染两轮均通过；完整核心测试 1235/1235，Release 0 warning / 0 error；UI 合同和格式检查通过 | 对齐 |

### 9.6 UI-R1E-B 显式启动入口修正记录

测试环境：Windows `10.0.26200`、Release x64、正式 `LongGrid.App.exe`、唯一 AppInstance、隔离配置目录；真实 Win32 顶层窗口枚举、真实第二进程激活重定向和 `WM_CLOSE` 关闭排空。

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 根目录/显式启动 | 用户主动打开软件应直接看到唯一正常控制中心 | 修正前实际只存在“Long方格桌面只读宿主”，控制中心为 0，用户看不到正常软件 UI；修正后显式启动在 1,245 ms 出现 1 个“Long方格”控制中心 | 已修正：默认启动标记为显式用户意图并激活控制中心 |
| 后台启动 | 自启动或明确后台场景不应强制弹出控制中心 | `--background` 实际在 1,158 ms 出现 1 个 DesktopHost、0 个控制中心，并连续响应 3 秒 | 对齐；后台能力由明确参数选择，不再与用户双击混用 |
| 单实例唤起 | 已后台运行时再次显式启动应退出第二进程并唤起现有控制中心 | 第二进程实际退出码 0，主进程随后出现 1 个控制中心，活动 LongGrid.App 数保持 1 | 对齐；继续复用正式 AppInstance 重定向链 |
| 退出与文件安全 | 关闭控制中心应完成排空，不留进程、不写隔离配置 | 两条路径最终活动进程数 0、临时配置写入数 0 | 对齐；没有读取、移动、重命名或删除桌面文件 |
| 视觉可见性 | 修正后的正式窗口应能被外部窗口工具观察 | Computer Use 修正前返回 0 个候选；修正后返回唯一“Long方格”窗口并捕获真实桌面概览截图 | 对齐可见性；自动键盘输入随后被用户物理 Escape 中止，结果记为未知且立即停止，不计入通过 |
| 回归 | 启动修正不得破坏 Core、构建、单实例与 UI | Release 0 warning / 0 error；核心测试由 1,235 增至 1,236 且 1,236/1,236；单实例 live 通过；真实 XAML 三图再次通过 | 对齐；高对比、关闭动画、Narrator、物理键盘和其他 DPI 继续 Pending |

### 9.7 BOX-R1 Explorer 背景菜单架构审计

代码实查显示 Passive DesktopHost 对普通桌面空白区返回 `HTTRANSPARENT`，只让“新建”按钮和标题区域接收输入；这是避免吞掉 Explorer 桌面选择、框选和右键的正确安全边界。现有 `HandleWorkspaceCreateContextMenu` 只接受“新建”按钮 Bounds，不能通过扩大透明 Surface 命中范围实现原始需求。

Microsoft 当前 Windows 11 正式路径是带应用身份的原生 `IExplorerCommand`，由包清单以 `windows.comServer` 和 `windows.fileExplorerContextMenus` 注册，并用 `Directory\Background` 覆盖文件夹/桌面背景。决定采用该路径，不使用全局 Hook、Raw Input、输入模拟、旧 `IContextMenu` 主路径或应用启动时静默写注册表。完整决策、恢复边界和 BOX-R1-A～D 见 [ADR-0003](adr/0003-desktop-background-context-menu.md)。

本轮真实差异：预期桌面任意空白处右键可见命令，实际生产代码只能在产品“新建”按钮区域显示菜单；I19 与 BSA ValidateOnly 实际均通过，但分别明确返回 `PendingManualEvidence`，没有把预检冒充人工通过。BOX-R1-C 还受未签名 Developer Preview MSIX 和可丢弃测试账户限制，当前禁止关闭系统安全策略强行安装。

| 本轮验证 | 预期 | 实际 | 差异/处理 |
|---|---|---|---|
| Release 核心套件 | 1,236 项零失败 | 1,236/1,236 通过，0 跳过 | 无差异 |
| 真实启动/单实例 | 显式启动唯一控制中心；后台启动唯一 DesktopHost；二次显式启动唤起控制中心；退出零进程、零临时配置写入 | 显式启动 2,302 ms；后台宿主 1,828 ms；二次进程退出码 0 且控制中心数 1；3 秒响应；最终进程 0、临时配置写入 0 | 无差异，均低于 10 秒预算 |
| UI 合同 | 正式 XAML 合同与唯一 AutomationId 集通过 | 184 个必需 AutomationId，结果 Pass | 无差异；不替代人工 Narrator/物理键盘证据 |
| Explorer 背景命令 | 桌面任意空白处出现“新建 Long方格盒子” | App 已能安全消费首进程/第二实例创建意图，但尚无原生命令 DLL、清单注册或已签名安装证据 | BOX-R1-B～C 待实现；禁止将 App 激活合同或静态清单计为菜单完成 |

### 9.8 BOX-R1-A 有限激活合同实测记录

正式 App 新增单一参数 `--long-grid-create-box=v1,x,y,issuedAtUnixMs,nonce`。解析器限制参数长度、版本、绝对坐标、30 秒有效期、5 秒未来时钟偏差和非空 GUID nonce；多命令、畸形、越界、过期、过度未来和重复 nonce 均有限拒绝。App 只使用坐标重新匹配当前权威 WorkArea，再以当前 workspace revision/topology generation 构造 `ExplorerContextMenu` 创建请求；成功路径只打开既有创建预览，不新增直接保存入口。

| 真实验证 | 预期 | 首次实际 | 修正后实际 |
|---|---|---|---|
| Release 编译 | 0 warning / 0 error | CA1859 阻断宽泛返回类型，随后接口合并暴露 C# 推导错误 | 收紧返回数组并显式提升证据接口后 0/0 |
| 首进程激活 | 首次 Shell 启动显示一次真实预览，取消零写入 | 首轮及 Windows PowerShell 5.1 复验都曾出现预览已驱动、窗口已激活但视觉树尚未加载，`PreviewVisualTreeCount=0`；固定 100 ms 延时不稳定 | 改为激活后在 2 秒总预算内每 25 ms 轮询真实 `IsProductXamlReady`，不无限等待、不伪造视觉树；连续 3 次 Initial 均预览/视觉树/激活为 1、盒子 0→0、Difference=None |
| 已运行单实例 | 第二进程退出 0，主进程显示一次预览 | 第二进程退出 0；预览/视觉树/激活均为 1 | 无产品差异 |
| 重复 nonce | 两个发送进程退出，但只消费一次 | 两个进程退出码均为 0，主进程预览次数 1 | 无差异，重复输入未生成第二预览 |
| 文件安全 | 隔离配置、用户正常配置与桌面元数据均不改变 | Initial / Redirect / DuplicateRedirect 三场景均为 false；最终活动进程 0 | 无差异 |
| 回归 | 普通显式/后台/单实例和 UI 合同保持 | 显式 4,965 ms、后台 3,261 ms、二次唤起通过、零临时配置写入；184-ID Pass；核心 1,247/1,247 | 无差异，均低于 10 秒启动预算 |
| CI 干净会话链 | 普通重定向与 BOX-R1 重定向分别跨入现有窗口 UI 队列 | 首次 CI 仍按拆分前的 `App.xaml.cs` 单一路径字符串检查，实际在 `App.BoxR1.cs` 已有两条正确调度路径，因此静态契约失败 | 将旧断言拆为普通激活与 BOX-R1 创建激活两项明确断言；本机两项契约均 Pass，新 CI 的干净会话链、单实例契约及其余门禁全部 Pass |
| 证据脚本宿主兼容 | 仓库声明的 Windows PowerShell 5.1 与 PowerShell 7 入口均可执行 | 首次用 `powershell.exe` 复验时，5.1 缺少 `Convert.ToHexString` 和 `ConvertFrom-Json -Depth`，脚本在产品启动/结果读取边界失败 | 改为 `BitConverter` 十六进制编码并使用兼容的 JSON 读取；5.1 下 Initial、Redirect、DuplicateRedirect 均 `Outcome=Pass / Difference=None`，7 下 ContractOnly Pass |

### 9.9 BOX-R1-B 原生命令与清单阶段记录

本阶段只完成可安装包中的原生 Shell 扩展工程和打包门禁，不安装未签名包、不修改包状态、不重启 Explorer。菜单真实可见、点击位置、多显示器和卸载清理仍属于 BOX-R1-C。

| 检查 | 预期效果 | 当前实际 | 差异与修正 |
|---|---|---|---|
| 原生工程 | x64 `IExplorerCommand` DLL 以编译器/链接器 `/W4 /WX` 编译 | 首次链接因 `.def` 的冗余 `LIBRARY` 输出名产生 2 个 LNK4070；删除该指令并启用链接器警告即错误后重建为 0 warning / 0 error；CLSID `78A940C1-2E65-4A03-9D09-3AC62CEF30BB` | 差异已修正，不接受“有警告但成功” |
| 菜单回调 | 标题、图标、Enabled、CanonicalName、默认 Flags 有界返回，无产品 I/O/等待 | 初版 `noexcept` 回调仍使用动态 `std::wstring`，极端 OOM 可能终止 surrogate；改为 257 字符以内固定缓冲、静态 CRT 与 CFG 后，真实 DLL 经 COM 类工厂创建，200 轮调用 0 ms；句柄 142→144（预算 ≤2），`DllCanUnloadNow=S_OK` | 安全差异已修正；不调用工作区、文件、网络、注册表或等待 API |
| 激活边界 | Invoke 只捕获当前光标、生成新 nonce/时间并调用包身份激活 | 源码合同使用唯一 AUMID 和 BOX-R1-A v1 参数；未安装包环境不执行 Invoke，避免意外改变包/应用状态 | App 端真实 Initial/Redirect/DuplicateRedirect 已由 BOX-R1-A 覆盖；Explorer→Invoke 留给 BOX-R1-C |
| MSIX 清单 | 同一 CLSID 同时进入 `com:SurrogateServer` 与唯一 `Directory\Background` Verb | 官方 MakeAppx 已接受清单；首次解包审计 XPath 漏掉 foundation namespace 的 `Extensions` 层而误报缺失；修正后真实包 `be1bc363...` 通过，双份解包布局指纹均为 `1f7439a9...`，CLSID/ItemType 正确 | XPath 已修正，并新增包内 DLL SHA-256 必须等于原生输出；`deterministicLayout=true`，MakeAppx 容器元数据导致 `byteReproducible=false` 如实保留 |
| 脚本兼容 | Windows PowerShell 5.1 能执行审计入口 | 首轮中文断言字面量被 5.1 按本地代码页解释为乱码 | 改为检查稳定符号名；C++ 仍以 `/utf-8` 编译真实中文标题，重跑合同与 DLL 探针 Pass |

## 10. 真实测试与差异修正规则

每个用户可见 PR 必须在描述和本文状态中记录：

| 字段 | 必填内容 |
|---|---|
| 预期效果 | 用户从什么状态执行什么动作，应看到什么结果 |
| 测试环境 | Windows build、DPI、主题、构建、真实输入方式 |
| 实际效果 | 实际窗口、操作、耗时、截图/录像/日志和数据结果 |
| 差异 | 与预期不一致的视觉、交互、性能、无障碍或数据事实 |
| 修正 | 本 PR 修正内容，未修正项的风险和明确后续编号 |
| 文件安全 | 操作前后文件数量、路径、哈希是否变化 |

禁止：仅凭 XAML 存在、AutomationId、静态合同、Mock、内存原型或截图成功声明产品完成。自动化负责防回归，真实键鼠/触控/Narrator/显示环境负责证明产品效果。

## 11. 开发节奏与文档瘦身

- 一个 PR 交付一个可演示的用户结果，建议控制在 1～3 个工作日；
- PR 开始前在本文领取当前编号，完成后只更新该编号和真实差异；
- 普通功能不新增编号 Stage 文档；证据写入 PR、测试结果和本文状态；
- 只有 R3/R4 平台风险、架构决策、协议版本或发布门禁需要专项审计/ADR；
- 不因为物理证据 Pending 无限开发新邻接底座；工程完成后必须安排产品证据冲刺；
- 未实现功能不得提前出现在主导航；
- 每周只报告 M1～M5 用户旅程完成度、当前阻断和下周唯一目标；
- `Complete` 只表示用户旅程、真实证据、自动化和安全门禁同时满足。

## 12. 当前完成度口径

- 工程底座：较强，PF-001～PF-006 已具备大量正式链路；
- GitHub 主分支交付：UI-R1A～UI-R1E-A 已按 #239 → #240 → #241 → #242 合入，文档收口 #243 与显式启动修正 #244 也已合入 `main@288838c`；#238 已作为重复路径关闭；
- M1 桌面盒子/文件夹绑定：未完成；BOX-R1-A/B、FOLDER-R1-A～D 与 PF-007A2 工程链已完成，BOX-R1-C、FOLDER-R1 和真实 Explorer 指针拖入证据 Pending；当前进入 PF-007B，随后执行 M1 集中物理证据；
- M2 任务栏美化核心：技术审计存在，产品运行时尚未开始；
- M3 桌面管理增强：未开始闭环；
- M4 工作空间/小组件/插件：协议或文档阶段；
- M5 正式交付：内部未签名链存在，正式发布未开始。

不再用“已经经历 207 个 Stage”表达进度。当前对用户诚实的状态是：**安全工程基础已形成，但桌面盒子、文件夹绑定和任务栏美化三项核心尚未共同完成。**

本次真实复测（2026-08-25）：Release 核心测试预期 1,235 项零失败，实际 1,235/1,235 通过；UI-R1E 预期生成浅色宽屏、浅色紧凑、深色宽屏三份正式 XAML 渲染，实际三份均通过，分别得到 `wide/LeftCompact`、`compact/LeftMinimal`、`wide/LeftCompact`，焦点均为 `ImportConfigurationButton`，桌面与正常配置元数据前后不变。实际系统为 `HighContrast=false`、`AnimationsEnabled=true`，因此高对比和关闭动画证据仍保持 Pending；证据会话 `1427c28ee34c4615a7834ce97476c8c6` 已通过精确清理，不保留测试沙箱。

主线集成复测（2026-08-26）：预期 Release 全解决方案零警告/零错误，实际 0/0；预期 UI 合同 184 IDs 通过，实际通过；预期三份正式 XAML 渲染通过，实际布局、主题、焦点和配置/桌面元数据均与 2026-08-25 一致。首次将完整核心测试与真实 WinUI 渲染并行执行时，Windows Shell `where.exe` 在 5 秒内未退出，实际为 1,234/1,235；该真实 Shell 项隔离复测 907 ms 通过，随后取消跨证据并行、顺序重跑完整核心套件，实际 1,235/1,235。差异判定为外部进程高负载下的测试编排时序干扰，不修改产品打开逻辑、不放宽断言；后续真实 Windows 进程/窗口证据必须与完整核心套件串行运行。高对比和关闭动画仍未在本机系统状态中发生，继续保持 Pending。

FOLDER-R1-A 真实文件系统记录（2026-08-26，基线 `origin/main@a0fe422`）：

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 单文件夹持久化 | 每个盒子至多保存一个路径与稳定身份；旧 v1/v2 配置无损升级且不能夹带 v3 字段 | schema v3 往返通过；v1/v2 自动升级为 v3；v2 夹带 `folderBinding` 被拒绝 | 首轮两处测试仍硬编码 schema v2，实际得到 v3；已统一改用当前版本常量并增加 v2 迁移/夹带拒绝测试，未放宽验证 |
| 真实目录身份 | 绑定真实目录后保存规范绝对路径、卷序列号和 128-bit 文件 ID | 真实临时 NTFS 目录探测成功；目录在同卷重命名/移动后，新路径身份与绑定身份完全一致 | 对齐；旧路径明确为 `Missing`，必须经后续显式重连更新目标，不静默搜索或改写配置 |
| 路径被替换 | 原目录删除且同路径出现新目录时不得误当原目标 | 实际解析为 `Replaced`，`ResolvedTarget=null`，替换目录内文件保持原文 | 对齐；路径和稳定身份必须同时匹配 |
| 解绑与无效目标 | 解绑、相对路径或文件目标不改变用户文件 | 解绑只改变内存配置状态；真实文件 SHA-256 前后一致；相对路径为 `InvalidTarget`、文件目标为 `NotDirectory` | 对齐；本阶段未触碰桌面目录或正常用户配置 |
| 回归与生成 | Release 全解决方案 0 warning / 0 error，核心测试零失败，格式无差异 | 实际 0/0；1,253/1,253；`dotnet format --verify-no-changes` 通过 | 对齐 |

远端 CI 首轮差异：预期进入完整测试，实际在既有 UI 隐私合同处提前失败，因为读模型曾把绑定持久化路径投影为 `FolderBindingTarget`；本地同命令精确复现。修正为读模型只暴露有限健康枚举，不携带持久化路径、卷号或文件 ID；随后同一批量无障碍合同通过。该修正保留“正式只读视图不泄露持久化身份”的原断言，没有修改 CI 工作流或放宽合同。

FOLDER-R1-A 的完成边界：配置合同、真实身份和健康状态已具备，启动会对持久化路径做权威解析；正式 UI 尚无“选择文件夹 / 解绑 / 重连”操作，也尚未把绑定目录内容投影到桌面盒子。产品对用户仍不可用，下一阶段不得重复建设身份探针。

FOLDER-R1-B 真实提交记录（2026-08-26，基线 `origin/main@e1fd469`）：

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 显式选择与取消 | 只在用户点击后打开系统 `FolderPicker`；取消必须在提交前返回且零写入 | 187-ID 正式 XAML/代码合同通过，确认 Picker 绑定窗口句柄、`folder is null` 分支先于提交并声明 `Changed=False` | 自动合同对齐；跨进程工具未完成实际取消点击，不能把这一行标为物理证据通过 |
| 绑定/重连 | 成功选择转换为 A 阶段稳定身份，并以当前盒子序号和共享修订提交；失效绑定可选择新目录覆盖 | `BindFolder` 只接受 `Resolved` 身份，走 `SetFolderBinding → Project → saves.Submit`；真实 NTFS 目录提交通过，路径/文件 ID 经真实配置 Store 重载一致 | 对齐；相同绑定为 NoChange，Picker 等待期间修订变化为 `StaleEditRevision` |
| 解绑与锁定 | 已绑定盒子可显式解绑；锁定盒子、夹带字段和无绑定解绑失败关闭 | `UnbindFolder` 只接受已绑定目标和显式确认；锁定返回 `ContainerLocked`，非绑定动作夹带身份返回 `InvalidRequest`，均零提交 | 对齐；解绑只清除 Long方格引用，不删除目录 |
| 保存失败与撤销 | 绑定和解绑复用已有一次撤销与保存失败补偿，不产生旁路写入 | 两个动作映射 `FolderBinding` undo kind，App 继续登记同一 `pendingControlCenterContainerEdit`，失败时经 `CommitContainerEditUndo` 恢复 | 对齐；PR #249 首轮完整 `build-test` 7m32s 通过，包括测试、覆盖率、文件安全、资源、依赖和 RC 门禁 |
| 用户文件安全 | 绑定、解绑、重连均不改变目录内容 | 真实临时目录的 `keep.txt` / `user.txt` 内容前后完全一致；真实 Store 只写隔离配置目录 | 对齐；未读取或写入用户桌面目录与正常配置 |
| 回归 | Release 编译、完整核心测试、格式、UI/无障碍链通过 | 本地 Release 0 warning / 0 error；1,256/1,256；格式通过；187-ID 和 BSA ValidateOnly 通过 | 自动化对齐；BSA 物理 Narrator/键盘仍按既有门禁 Pending |

真实窗口差异：预期由 Windows 应用控制取得“选择文件夹 → 取消 → 状态文本”证据；实际唯一 `Long方格` Release 窗口成功启动，但捕获时报 `foreground window did not report a process id`，重新枚举时窗口已退出。没有重用旧坐标或伪造点击，物理 Picker 证据保持 `ManualPickerEvidencePending`。本阶段的真实效果证据因此限定为真实 NTFS 身份、真实配置 Store 和用户文件不变；可见控件为工程完成，不等于人工交互完成。

FOLDER-R1-B 的完成边界：用户界面和原子提交链已经存在，能表达未绑定、可访问、缺失、权限拒绝、路径被替换和不可用状态；它尚未枚举或显示绑定目录内容，也没有刷新/打开闭环。下一阶段不得转去任务栏、小组件或自动整理。

FOLDER-R1-C 真实文件系统记录（2026-08-26，基线 `origin/main@8bf2dff`）：

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 直属内容投影 | 仅对 `Resolved` 绑定枚举直属文件和子目录，不递归、不写配置、不移动文件，并同时出现在盒子管理页与 DesktopHost | 真实临时 NTFS 目录中的子目录和文件按“目录优先、名称稳定排序”进入同一读模型；嵌套文件未出现；DesktopHost 使用 `folder:{generation}:{ordinal}` 临时 ID | 对齐；投影目标仅保留在运行时快照，正式 UI 只接收名称、类型、来源和有限状态 |
| 有界与失败关闭 | 大目录最多展示 256 项；重解析点跳过；路径替换、权限、长路径或枚举中断不得发布部分可信结果 | 260 个真实文件实际投影 256 个并返回 `Truncated`；同路径替换实际返回 `BindingUnavailable` 且 0 项；异常映射到 `AccessDenied / InvalidTarget / EnumerationFailed` | 对齐；畸形临时快照初次可绕过旧 UI 合同的宽泛 `.Id` 禁止规则，已改为验证代次 ID、唯一性、名称、类型和绝对目标，并保持禁止持久化身份/目标进入读模型 |
| 刷新与变化通知 | 显式刷新和目录直属项创建、删除、重命名后重新读取；旧代次不能打开 | 实际显式第二代读取显示新增文件；真实 `FileSystemWatcher` 创建通知在 5 秒门限内到达，经 250 ms 去抖触发新代次；旧 `folder:1:*` 请求面对第二代快照返回 `TargetUnavailable` | 对齐；监听器不可建立时不伪报自动刷新，保留显式刷新入口 |
| 安全打开 | 打开前重新验证根目录稳定身份、直属关系、当前类型和重解析点；失效或类型变化不启动 | 真实文件由记录型 Shell 启动器收到规范绝对路径；文件内容和最后写入时间不变；文件被目录替换后实际为 `TypeChanged` 且 0 次启动 | 对齐；正式运行继续复用既有系统 Shell 启动器，快捷方式和 URL 仍走原安全解析器 |
| 用户文件安全 | 枚举、刷新、变更通知和打开预检前后文件哈希与位置保持不变 | 真实目录项目集、SHA-256、文件内容和父目录前后逐项一致；替换目录内容未被读取为原绑定，也未被修改 | 对齐，`Difference=None` |
| 回归与 UI 合同 | Release 全量测试、构建、格式、覆盖率和正式 UI 合同通过，不把绑定目录项目混入引用移除/改归属 | 新增专项 8/8；全量 1,264/1,264；Release 构建 0 warning / 0 error；格式通过；隔离覆盖率 lines 90.34% / branches 75.96%；188-ID UI 合同及 BSA ValidateOnly 通过，并要求 `BoundFolder` 来源从移除/改归属候选中排除 | 首轮 UI 合同仍以“读模型不得出现任何 `.Id`”阻断临时不透明 ID，已改为精确禁止持久化身份；首轮覆盖率误聚合工作区 27 份历史报告得到 86.30% / 73.54%，改用不可复用的独立结果目录后通过，未降低门槛 |
| 可见物理证据 | 启动正式 Release 窗口验证刷新按钮、内容列表和桌面双击 | 完整 UI 脚本在启动应用前检测到已审计的 WindowsAppRuntime `2.4.0.0` / Microsoft.UI.Xaml `3.2.3.0` 跨进程 UIA `RPC_E_WRONG_THREAD` fail-fast 组合并安全阻断；未传入风险确认，未启动应用、未伪造点击 | `ProductEvidencePending`；结构与真实文件系统通过不替代可见证据，待上游稳定运行时或可丢弃诊断账户补证 |

FOLDER-R1-C 的完成边界：内容投影、显式刷新、真实变更通知、桌面呈现和打开前重验证已具备；刷新生命周期已提取到独立 `App.FolderContents.cs`，没有继续把约 150 行协调逻辑集中进 `App.xaml.cs`。尚未完成权限实时切换、根目录离线/恢复、Watcher 溢出恢复、可见刷新/打开截图和物理键盘/Narrator 证据，因此保持 `ProductEvidencePending`，不得把 C 写成完整文件夹产品旅程已完成。

FOLDER-R1-D 权限、离线与失效恢复记录（2026-08-26，基线 `origin/main@803299c`）：

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 权威有限状态 | 运行时目录变化后，控制中心与 DesktopHost 使用同一份 `Resolved / Missing / AccessDenied / Replaced / Unavailable` 事实，不得继续显示保存时的旧 `Resolved` | 首轮代码审计发现 C 的内容读取器会重新验证目录，却没有把新绑定状态送入读模型，可能同时显示“绑定正常”和“内容不可用”；D 在临时内容快照加入有限 `BindingResolution`，读模型只接受形状有效的新代次状态 | 已修正事实矛盾；正式视图仍不暴露路径、卷号或文件 ID，临时快照不进入配置存储 |
| 根目录离线与归来 | 绑定目录被同卷移走后自动进入 `Missing`，原目录回到同一路径后自动恢复 `Resolved` 和原直属项目 | 真实 NTFS 目录经 `Directory.Move` 离线，父目录监听在 5 秒门限内触发；移回后再次触发并恢复 `keep.txt`，内容 SHA-256 前后一致 | C 只监听在线根目录，根目录消失后无法可靠观察归来；已增加持久化目标的父目录精确名称监听，并继续保留根目录监听与 250 ms 去抖 |
| ACL 拒绝与恢复 | 当前用户权限从允许变为拒绝后自动进入 `AccessDenied` 且不显示内容；移除拒绝规则后自动恢复 | 使用真实 `icacls` 对隔离目录添加当前 SID deny：第一次预期 5 秒内收到 `FileSystemWatcher` 安全通知，实际超时；加入每 2 秒一次且禁止重叠的稳定身份权威健康探测后，拒绝与恢复两次失效通知均到达，读取结果依次为 `AccessDenied → Resolved`，文件哈希不变 | 差异已修正；健康探测去重相同绑定且只比较有限状态，不枚举内容、不写配置、不记录路径或身份；显式刷新仍作为手动恢复入口 |
| 同路径替换 | 原目录被删除并在同一路径创建新目录时，不能显示替换目录内容 | 真实同路径替换继续得到 `Replaced`、0 项；读模型同步显示 `Replaced`，替换目录文件原文不变 | 对齐；没有按路径静默接受新身份 |
| Watcher 错误恢复 | `FileSystemWatcher.Error` 或缓冲区溢出不能让旧状态永久卡住 | 正式 `Error` 路径与文件事件统一安排去抖失效重读；低频权威探测可在事件丢失时继续发现有限健康变化 | 代码和 188-ID 合同通过；本机未安全、确定地制造真实内部缓冲区溢出，因此物理 overflow 证据保持 Pending，不伪造 Pass |
| 回归与文件安全 | 新恢复链不移动、删除、重命名或改写用户文件；Release、全量测试、覆盖率、格式与 UI 合同通过 | 专项 8/8；全量 1,266/1,266；Release 0 warning / 0 error；最终隔离覆盖率 lines 90.38% / branches 76.05%；格式与 188-ID 合同通过。离线测试只执行并完整恢复显式测试目录重命名，ACL 测试在 `finally` 移除 deny；保护文件哈希一致 | 对齐；没有触碰桌面目录、正常用户配置或系统任务栏设置 |
| 可见物理证据 | 正式窗口显示拒绝、离线和恢复状态，并完成真实键盘/Narrator 检查 | 完整 UI 脚本在应用启动前再次识别 WindowsAppRuntime `2.4.0.0` / Microsoft.UI.Xaml `3.2.3.0` 的已审计 `RPC_E_WRONG_THREAD` fail-fast 组合并安全停止；BSA ValidateOnly 明确返回 `PendingManualEvidence` | `ProductEvidencePending`；未传风险绕过参数、未启动应用、未伪造点击或截图，待上游稳定运行时或可丢弃诊断环境在 M1 集中补证 |

FOLDER-R1-D 的完成边界：真实权限拒绝/恢复、根目录离线/归来、同路径替换拒绝、事件丢失补偿和有限权威状态已形成工程闭环；监听器只读探测，内容枚举仍有界且不持久化。物理 overflow、可见状态、Picker、刷新/打开、键盘和 Narrator 仍待 M1 证据冲刺，因此 D 只能标为 `EngineeringComplete / RealFilesystemPass / ProductEvidencePending`，不能标为产品旅程 Complete。

PF-007A2 DesktopHost OLE DropTarget 记录（2026-08-26，基线 `origin/main@53c8b5e`）：

| 检查 | 预期效果 | 实际效果 | 差异与修正 |
|---|---|---|---|
| 正式原生注册 | 每个正式 DesktopHost HWND 在 STA/OLE 上注册 `IDropTarget`，销毁前撤销；注册失败时不接受拖放 | 新适配器在窗口创建后调用 `OleInitialize → RegisterDragDrop`，Surface 销毁前 `RevokeDragDrop → OleUninitialize`；真实独立 STA 创建真实 HWND，实际注册成功 | A1 完全没有注册路径；已补齐，且 MTA/注册失败安全返回不可用，不影响既有桌面宿主启动 |
| 悬停语义 | 只对 CF_HDROP、允许 Link、可见且未锁定的盒子返回 `DROPEFFECT_LINK`，不能暗示 Copy/Move | 真实 CF_HDROP 在盒子范围实际返回 Link，DragEnter 零业务回调；非 HDROP、Copy-only 和 Hidden Surface 实际均返回 None；命中盒子绘制有限焦点框 | 对齐；Ctrl/Shift 不改变安全引用命令形状，锁定和权威 Catalog 门禁继续由 A1/投影双重拒绝 |
| Drop 与提交 | Drop 只提交一次，并复用当前编辑修订、权威 Catalog、原子批量引用、保存失败补偿和一次撤销 | 正式 Surface → Lifecycle → App → `Prepare` → `CommitResolvedReferenceBatch` 链已接通；真实 STA/HWND 场景实际回调 1 次、保存 1 次、盒子增加 1 个引用 | 对齐；COM 回调异常失败关闭为 None，不越过现有事务链 |
| 文件安全 | 安全 Link 不移动、复制、删除、重命名或改写源文件 | 真实隔离 Unicode 文件以真实 CF_HDROP HGLOBAL 投递；投递前后规范路径和 SHA-256 相同 | `Difference=None`；代码与 UI 合同同时禁止文件变更 API进入 DropTarget |
| 回归 | 专项、全量、Release、格式、覆盖率与 UI 合同通过 | 专项 9/9，真实 STA/OLE 场景连续 3/3；全量 1,267/1,267；Release 0 warning / 0 error；最终 lines 90.35% / branches 75.93%；格式和 188-ID UI 合同通过 | 对齐，未降低门槛 |
| 产品物理证据 | 用户从真实 Explorer 用鼠标拖到桌面盒子，看到 Link 光标/轮廓并出现引用 | 本轮使用真实 STA、真实 HWND、原生 RegisterDragDrop 和真实 CF_HDROP，但最终回调由证据入口驱动，不是物理鼠标；未将其伪报为物理 Explorer 拖入 | `ProductEvidencePending`；与 BOX/FOLDER/UI 人工项一起进入 M1 集中证据 |

PF-007A2 的完成边界：正式产品已经拥有原生 OLE DropTarget 和端到端安全引用提交工程链，真实注册、有限效果、一次提交及文件零变化已证明；尚未取得物理 Explorer 指针和屏幕可见证据，因此不能标为用户旅程 Complete。下一批只做 PF-007B 盒子间改归属，不再扩展 A2 邻接底座。

## 13. 下一步开始条件

下一次开发进入 PF-007B：复用现有选择、目标盒子命中、`CommitResolvedReferenceReassignment` 和一次撤销合同，建立盒子内项目拖到另一盒子的有限内部拖放协议。必须拒绝同源目标、锁定源/目标、BoundFolder 临时项目、失效引用、修订/代次漂移和超过 256 项；保存失败必须恢复源/目标原状态。测试继续使用真实隔离文件并比较路径和 SHA-256，原生 HWND 拖放应记录预期、实际和差异；不得把 reducer 单测冒充完整产品链。B 完成后进入 M1 产品证据冲刺，集中补齐物理 Explorer 拖入、BOX-R1-C、FOLDER-R1 与 UI-R1E 的可见、键盘和 Narrator 证据。
